# Release-review fix batch report (2026-08-23)

## Dispositions

1a. `src/pkjs/index.js` `sendWeatherDict` — reply send now has bounded retries:
    `doSendWeatherDict` (2 retries, 5 s apart, fresh fn per attempt, final log).
    Cache write unchanged.
1b. `src/c/main.c` — outbox-reset kept; new `weather_request_answered()` (same
    shape) resets the same counter; messaging.c's weather-payload receipt path
    calls it. main.h gained one export.
1c. geolocation error handler bails without retry on `PERMISSION_DENIED` (logs),
    other errors keep the existing retry cadence.
1d. `weatherFetchInFlight` guard added at `getWeather` entry; dropped at all
    terminals (send success, send final failure, retry exhaustion, permission
    bail). Ready-listener incomplete-cache refetch and the `:appmessage`
    listener both route through it; concurrent triggers log and no-op.
2.  `src/c/messaging.c` — `clamp_weather_values()` (sentinel-aware, so absent
    readings stay "--") applied after field parsing on real payloads, before
    persist. Bounds mirror the paint budget; wind speed capped 999 to match the
    formatter's existing cap.
3.  README settings table: vibration row removed (feature is gone); CRT effect
    and CRT degauss sound rows added ("On, off (default)" — the defaults after
    today's flip).
4.  `src/c/data.h` retired-ids comment corrected (33 is WEEK_NUMBER, live since
    1.6.0 — not renumbered). contract-scrape `dataSourceIds()` now also asserts
    numeric-id uniqueness (name AND value collisions hard-fail).
5a. ISSUES.md: new entry linking the underrun doc, in list style.
5b. Landed plan files' `- [ ]` boxes marked `- [x]` (crt-subpixel,
    crt-radial-curvature, crt-review-round).
5c. degauss-field plan now carries a Status line under the header: Task 3's
    sound work is superseded by the underrun findings (link included); Tasks
    1/2/4 remain pending candidates.

Explicitly not done: `save_weather_cache` order — timestamp-last-and-unconditional
IS the torn-write guard; no change.

## Hairy moments

- My first clamp-table draft clamped the -999/-1 sentinels into valid readings —
  absolute value clamps would have painted missing data as 999. Fixed by making
  the helper sentinel-aware before any test ran.
- The C harness caught nothing structural beyond that; the JS tests needed the
  config count pin (9 → 8) after the README-adjacent change was the settings
  table only — that pin moved in the same session.

## State

- `make test`: 178/178 C tests, format-clean, JS suite green (after box count)
- No emulator gate run — nothing here touches the render path
- No jj mutations; no commits
- Changed: src/pkjs/index.js, src/c/main.c, src/c/main.h, src/c/messaging.c,
  src/c/data.h, README.md, test/test_watchface.c, test/pkjs/config.test.js,
  test/pkjs/contract-scrape.js, ISSUES.md, docs/superpowers/plans/* (3 landed,
  plus the status line on the degauss plan)
